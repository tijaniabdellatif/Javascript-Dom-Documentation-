//get element by ID 
const title = document.getElementById('title');
console.log(title);

//get element By class name we can not use Foreach on HTMLcollection
const clas = document.getElementsByClassName('err');
console.log(clas);


//get element by tag name

const tag = document.getElementsByTagName('p');
console.log(tag);
console.log(tag[1]);

